import image from "./images";
import { COLORS, typography,  } from "./theme";
import { splashData } from "./data";
import  icons  from "./icons";
import { onboardingOptions } from "./data";

const appMode = {
    dark: "dark",
    light: "light"
}

export { image, COLORS, typography, splashData, icons, appMode, onboardingOptions };